import { Deal } from '@/types';

export const DealsList = () => {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {/* Deal items will be rendered here */}
    </div>
  );
};
